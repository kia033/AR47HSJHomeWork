1. 코드영역
	상수
	모든 함수의 설계도 <= 어떤 함수를 보건 그 설계도의 내용은 무조건 코드 영역이다.
	ex) 이 함수가 실행되면 이러이러한 일을 할거야

	사용자 자료형의 설계도들
	ex) 이 클래스가 만들어지면 이러이러한 일을 해야해 

	// 나머지 영역은 전부다 어떠한 변수들의 메모리가 된다.

2.데이터 영역
// 전역변수 데이터들이 존해한다.

3. 힙영역

4.스택영역
// 운영체제가 알아서
// 보통 1메가면 충분하다. <= 이 크기도 조절할 수 있는데 안한다.

	함수의 설계도 대로 실행되는 실행흐름의 메모리가 존재하는 곳.
	새로운 실행흐름이 실행되면 메모리가 할당되었다가
	종료되면 메모리가 소멸하는 곳
	 
